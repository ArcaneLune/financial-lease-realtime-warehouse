# 金融租赁授信审批实时数仓（项目介绍）

> 配套文档：`README.md`（项目总览）、`doc/02-数仓建模开发.md`（建模步骤/命令/踩坑）、`doc/业务理解与指标处理.md`（业务总线矩阵/指标体系）、`doc/03-FineBI可视化制作.md`。

## 一、项目概述

本项目围绕金融租赁授信审批业务搭建了一套实时数据仓库，基于 3 节点 CentOS 7 虚拟机集群，采用 Flink CDC + Kafka + Doris 技术栈，构建 ODS → DIM → DWD → DWS → ADS 五层，并用 FineBI 输出经营分析驾驶舱。

- **数据采集**：Flink CDC 单作业整库读取 `financial_lease` 业务库全部 9 张表，以 debezium-json 格式按 `source.table` 动态路由写入 Kafka（`ods_表名`）。
- **数仓建模**：Flink DataStream API 完成五层建模，Doris 统一存储。
  - DIM：4 张维度表（部门/员工/行业/商业合伙人，raw mirror）
  - DWD：8 张事务事实表（审批通过/拒绝/取消、新增授信/占用、合同制作/签约/起租）
  - DWS：11 张窗口聚合表（10 秒滚动窗口，AGGREGATE KEY + 维度拍平）
  - ADS：7 张 Doris 物化视图（已审项目 6 粒度 + 转化漏斗，5 分钟自动刷新）
- **可视化**：FineBI 对接 Doris `ads` 库，输出 KPI 卡片 + 授信漏斗 + 多维报表。

## 二、项目背景与价值

### 2.1 为什么要做这个项目？

项目模拟的是金融租赁公司的授信审批业务：客户（承租人）向租赁公司申请授信额度，用于购买设备等资产后「以租代购」。这类业务对数据时效的要求很高：

1. **审批链路长**：一笔授信要经过风控初审、信审、一级/二级评审、项目评审会、总经理逐级审批，节点多、流转复杂，管理层需要实时掌握每个项目卡在哪个环节。
2. **资金体量大、周期长**：从申请到起租往往以月计，任何环节的延误都直接意味着资金占用和风险敞口。
3. **监管要求严**：授信全流程要求留痕、可追溯、准实时报送，欺诈识别、额度超限这类风控场景更是要秒级响应。

这些诉求传统 T+1 数仓满足不了：数据要次日才能看到。所以本项目用 Flink + Doris 搭建实时数仓，把授信审批数据从产生到可视化的延迟压到秒级。

### 2.2 解决了什么问题

| 业务痛点 | 传统 T+1 数仓 | 本项目实时数仓 |
|---------|--------------|---------------|
| 审批进度不可见，问题环节无法及时干预 | 次日才能看到流转结果 | 秒级掌握每个项目所处审批节点 |
| 授信转化漏斗无法实时跟踪 | 月底才出报表 | 10 秒窗口实时刷新申请→批复→占用→签约→起租漏斗 |
| 经营指标滞后，决策慢 | 日报 / 月报 | DWS 10 秒滚动聚合 + ADS 5 分钟物化视图，大屏秒级刷新 |
| 监管报送不及时 | 手工汇总 | 全链路流水可追溯，准实时输出 |
| 维度变更同步慢 | 维度表每日刷新 | 维度表实时 mirror，DWD/DWS 实时关联 |

## 三、业务逻辑说明

### 3.1 业务场景

项目模拟的业务是金融租赁授信审批：承租人（企业客户）向租赁公司申请授信，经多级审批后拿到额度，随后完成授信占用、签订合同并起租。全流程围绕主表 `credit_facility`（授信申请）展开，配合多张流转表、明细表记录每个环节。

### 3.2 授信生命周期（状态机）

```
授信申请 → 多级审批 → 出具批复（status=16） → 新增授信 → 授信占用 → 合同制作 → 签约 → 起租
                       ├── 拒绝（status=20）─┘
                       └── 取消（status=21）─┘
```

| 环节 | 业务含义 | 数据来源（如何识别） |
|------|---------|---------------------|
| 授信申请 | 客户提交授信申请 | `credit_facility` 插入 |
| 多级审批 | 风控 → 信审 → 一级/二级评审 → 项目评审会 → 总经理逐级审核 | `credit_facility_status` 流转记录 |
| 审批通过 | 出具批复 | 流水表 `status = 16` |
| 审批拒绝 | 风控 / 信审直接拒绝 | 流水表 `status = 20` |
| 审批取消 | 客户 / 业务主动取消 | 流水表 `status = 21` |
| 新增授信 | 批复后生成授信额度 | `credit` 插入 |
| 授信占用 | 客户实际支用额度 | `credit.occupy_time` 非空 |
| 合同制作 | 生成租赁合同 | `contract.produce_time` 非空 |
| 签约 | 双方签署合同 | `contract.signed_time` 非空 |
| 起租 | 合同生效、开始计租 | `contract.execution_time` 非空 |

> 口径备注（开发时踩过坑才确定的）：审批是否通过、授信是否占用、合同是否起租，要看流水记录和时间字段，不能看主表 `status` 的当前值。状态是会继续流转的，比如 16（出具批复）会流转到 19（新增授信），只看当前值会漏掉大部分数据。

### 3.3 核心业务指标

| 指标 | 口径 | 层级 |
|------|------|------|
| 已审项目数 | 审批通过 / 拒绝 / 取消的授信申请数 | ADS（6 粒度） |
| 审批金额 | 申请金额 / 批复金额（apply_amount / reply_amount） | DWD / ADS |
| 授信转化漏斗 | 新增授信 → 占用 → 制作 → 签约 → 起租 各环节项目数 | ADS 转化主题 |
| 10 秒实时指标 | 窗口内申请 / 批复笔数与金额 | DWS（11 张窗口表） |
| 维度画像 | 行业 / 部门 / 业务经办 / 信审经办 粒度分布 | ADS 多粒度物化视图 |

## 四、架构设计

### 4.1 整体架构

```
模拟数据源
   │  mock-finance-1.3.0.jar 向 MySQL 注入业务数据
   ▼
MySQL（金融租赁业务库 financial_lease，9 张表）
   │  Flink CDC 2.4.2 采集 binlog（单作业整库读）
   ▼
Kafka（ODS 层，debezium-json，按 source.table 路由到 ods_表名）
   │  Flink 1.17.1 DataStream API
   ▼
Doris 2.1.0（数仓建模：DIM / DWD / DWS / ADS 四层）
   │  ADS 用异步物化视图，5 分钟自动刷新
   ▼
FineBI（经营分析驾驶舱 / 大屏）
```

### 4.2 分层说明

| 层级 | 存储组件 | 加工组件 | 作用 |
| --- | --- | --- | --- |
| 数据源 | MySQL | mock 模拟数据 | 金融租赁业务库原始数据（9 张表） |
| ODS 层 | Kafka | Flink CDC | 全库 binlog 实时接入，原始数据零加工 |
| DIM 层 | Doris | Flink | 4 张维度表 raw mirror |
| DWD 层 | Doris + Kafka | Flink | 8 张事务事实表，单作业动态分流、双写 |
| DWS 层 | Doris | Flink | 11 张窗口聚合表，10 秒滚动窗口 |
| ADS 层 | Doris | Doris 物化视图 | 7 张应用指标表，5 分钟自动刷新 |
| 可视化 | Doris + FineBI | - | KPI 卡片 / 授信漏斗 / 多维报表 |

### 4.3 技术选型

| 组件 | 选型 | 版本 | 用途 |
| --- | --- | --- | --- |
| 业务数据库 | MySQL | 8.0.39 | 金融租赁业务源库 |
| 数据采集 | Flink CDC | 2.4.2 | 实时捕获 MySQL Binlog |
| 消息队列 | Kafka | 2.12-3.3.1 | ODS 层数据缓冲与解耦 |
| 流计算引擎 | Flink | 1.17.1 | 全链路实时加工 |
| 实时数仓 | Apache Doris | 2.1.0 | DIM/DWD/DWS/ADS 统一存储 |
| 协调服务 | ZooKeeper | 3.7.1 | Kafka 元数据协调 |
| 数据可视化 | FineBI | - | 经营分析驾驶舱 |

## 五、数仓分层设计

### 5.1 ODS 层（原始数据层）

- 存储：Kafka Topic
- 数据来源：Flink CDC 单作业整库读取 MySQL Binlog
- 特点：保留原始数据结构，不做任何加工，作为后续层的统一数据入口
- Topic 命名规范：`ods_表名`（按 `source.table` 动态路由，共 9 个）

### 5.2 DIM 层（维度层）

- 存储：Doris（`dim` 库）
- 数据来源：Flink 消费 Kafka ODS Topic
- 内容：4 张维度表（部门 / 员工 / 行业 / 商业合伙人）
- 特点：raw mirror 原始镜像，保留 `superior_department_id` / `superior_industry_id` 等层级字段，3 级拍平统一在 DWS 层用广播状态完成
- 建模方式：Doris Unique Key 模型

### 5.3 DWD 层（明细数据层）

- 存储：Doris（`dwd` 库）+ Kafka（双写，供 DWS 实时消费）
- 数据来源：Flink 单作业消费 5 张 ODS Topic（credit / credit_facility / credit_facility_status / reply / contract），按 `credit_facility_id` 分组，事件检测后侧输出分流
- 内容：8 张事务事实表，覆盖审批 / 授信 / 租赁三域
- 特点：保留项目级明细，是 DWS / ADS 层的基础
- 建模方式：Doris DUPLICATE KEY + 按天动态分区

### 5.4 DWS 层（汇总数据层）

- 存储：Doris（`dws` 库）
- 数据来源：Flink 消费 DWD 双写的 Kafka 数据，10 秒滚动窗口聚合
- 内容：11 张窗口聚合表（无维度 5 张 + 带维度 6 张）
- 特点：AGGREGATE KEY + REPLACE 幂等写，维度广播拍平（行业 / 部门 3 级 + 员工姓名）
- 建模方式：Doris AGGREGATE KEY 模型

### 5.5 ADS 层（应用数据层）

- 存储：Doris（`ads` 库）
- 数据来源：Doris 异步物化视图，直接从 DWD + DIM 聚合
- 内容：7 张物化视图（已审项目 6 粒度 + 转化漏斗）
- 特点：`BUILD IMMEDIATE` + `REFRESH AUTO ON SCHEDULE EVERY 5 MINUTE`，自动维护、FineBI 直查
- 建模方式：Doris 异步物化视图

## 六、数据流转示例

以一笔授信申请从提交到起租为例：

1. 模拟程序向 MySQL 写入授信申请，随后触发多级审批流转。
2. Flink CDC 捕获 Binlog，按 `source.table` 动态路由写入 Kafka `ods_*` Topic。
3. Flink DIM 作业消费 `ods_*`，将部门 / 员工 / 行业 / 商业合伙人实时镜像到 Doris `dim` 库。
4. Flink DWD 作业消费 5 张 ODS Topic，按 `credit_facility_id` 分组、事件检测，分别输出到 8 张事实表（Doris）+ 8 个 Kafka Topic。
5. Flink DWS 作业消费 DWD 双写数据，10 秒滚动窗口聚合写入 Doris `dws` 库。
6. Doris 异步物化视图每 5 分钟从 DWD + DIM 刷新 `ads` 库。
7. FineBI 直查 `ads` 库输出经营分析驾驶舱。

## 七、项目交付物

| 交付物 | 说明 |
| --- | --- |
| README.md | 项目总览（架构 / 分层 / 业务逻辑 / 快速开始） |
| 项目介绍.md | 本文档（背景 / 价值 / 业务逻辑 / 设计） |
| flink_test/ | Flink 数仓建模代码工程（DataStream API，五层实现） |
| sql/ | dim / dwd / dws / ads 四层建表语句 |
| scripts/ | Linux 集群启停 / 分发脚本（zk / kf / hdp / doris-cluster / xsync / xcall） |
| doc/ | 从 0-1 搭建教程（环境搭建 / 建模开发 / 业务理解与指标处理 / FineBI 可视化） |
| 模拟数据脚本/ | mock-finance-1.3.0.jar 数据生成器 |
| images/ | 架构图 + 项目截图 |

## 八、项目目标

- 构建端到端金融租赁授信审批实时数仓，数据从产生到可视化的端到端延迟控制在秒级。
- 形成一套可复用的 Flink + Doris 五层分层模板，可迁移到其他金融业务域。
- 形成「实时风控 + 实时大屏 + 准实时监管报送」的完整实时数据底座。
