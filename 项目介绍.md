# 金融实时数仓项目介绍文档

## 一、项目概述

本项目是一个基于金融业务场景的实时数据仓库建设方案，旨在通过流批一体化处理架构，实现对金融交易数据的实时采集、加工、聚合与展示。项目以实时性为核心目标，覆盖从数据源接入到最终可视化呈现的完整链路，为金融风控、运营监控、实时报表等业务场景提供数据支撑。

## 二、项目背景

金融行业对数据的时效性要求极高，传统 T+1 数仓已无法满足以下业务诉求：

- 实时风控：交易欺诈识别需要在秒级响应。
- 实时大屏：运营监控大屏需要秒级刷新指标。
- 实时报表：业务部门需要实时查询当日交易概况。
- 监管报送：部分监管数据要求准实时上传。

因此，建设一套以 Flink + Doris 为核心的实时数仓，是金融数字化转型的关键基础设施。

## 三、架构设计

### 3.1 整体架构

整体数据流向如下：

```
模拟数据源 → MySQL → Flink CDC → Kafka(ODS) → Flink → Doris(DIM/DWD/DWS/ADS) → 可视化
```

分层说明：

| 层级 | 存储组件 | 加工组件 | 作用 |
| --- | --- | --- | --- |
| 数据源 | MySQL | 模拟数据生成 | 业务库原始数据 |
| ODS 层 | Kafka | Flink CDC | 原始数据实时接入 |
| DIM 层 | Doris | Flink | 维表数据存储 |
| DWD 层 | Doris | Flink | 明细数据清洗 |
| DWS 层 | Doris | Flink | 汇总数据聚合 |
| ADS 层 | Doris | Flink | 应用指标计算 |
| 可视化 | Doris + BI 工具 | - | 数据展示 |

### 3.2 技术选型

| 组件 | 选型 | 用途 |
| --- | --- | --- |
| 业务数据库 | MySQL | 模拟金融业务源库 |
| 数据采集 | Flink CDC | 实时捕获 MySQL Binlog |
| 消息队列 | Kafka | ODS 层数据缓冲与解耦 |
| 流计算引擎 | Flink | 全链路实时加工 |
| 实时数仓 | Doris | DIM/DWD/DWS/ADS 统一存储 |
| 可视化 | Doris + 可视化工具 | 指标展示 |

## 四、数仓分层设计

### 4.1 ODS 层（原始数据层）

- 存储：Kafka Topic
- 数据来源：Flink CDC 实时采集 MySQL Binlog
- 特点：保留原始数据结构，不做任何加工，作为后续层的统一数据入口
- Topic 命名规范：`ods_<业务域>_<表名>`

### 4.2 DIM 层（维度层）

- 存储：Doris
- 数据来源：Flink 消费 Kafka ODS Topic
- 作用：存储维度数据，如用户维表、机构维表、产品维表、地区维表等
- 特点：用于 DWD/DWS 层关联补充维度字段
- 建模方式：Doris 明细模型或主键模型

### 4.3 DWD 层（明细数据层）

- 存储：Doris
- 数据来源：Flink 消费 ODS Topic 并关联 DIM 维表
- 作用：对原始数据进行清洗、标准化、维度补全，生成标准化明细宽表
- 特点：保留交易级别明细数据，是 DWS/ADS 层的基础
- 建模方式：Doris 明细模型

### 4.4 DWS 层（汇总数据层）

- 存储：Doris
- 数据来源：Flink 消费 DWD 数据进行窗口聚合
- 作用：按业务主题、时间粒度（分钟、小时、天）汇总指标
- 特点：预计算常用聚合指标，提升 ADS 层查询性能
- 建模方式：Doris 聚合模型或主键模型

### 4.5 ADS 层（应用数据层）

- 存储：Doris
- 数据来源：Flink 消费 DWS 数据或直接聚合 DWD
- 作用：面向应用场景输出最终指标，如实时大屏、风控特征
- 特点：直接对接可视化与业务系统
- 建模方式：Doris 聚合模型

## 五、业务场景

本项目模拟金融业务数据，覆盖以下典型场景：

1. 交易明细实时入仓：每笔交易明细秒级进入 DWD 层。
2. 实时风控特征：基于 DWD 计算用户近 N 分钟交易频次、金额阈值。
3. 实时交易大屏：当日交易总额、笔数、活跃用户数秒级刷新。
4. 机构维表实时同步：机构信息变更实时同步到 DIM 层。

## 六、数据流转示例

以一笔交易为例：

1. 模拟程序向 MySQL 写入交易记录。
2. Flink CDC 捕获 Binlog 并写入 Kafka ODS Topic。
3. Flink 任务消费 ODS Topic，关联 DIM 维表，清洗后写入 Doris DWD 表。
4. Flink 任务对 DWD 数据按分钟聚合，写入 Doris DWS 表。
5. Flink 任务对 DWS 数据计算最终指标，写入 Doris ADS 表。
6. 可视化工具查询 Doris ADS 表进行展示。

## 七、项目交付物

| 交付物 | 说明 |
| --- | --- |
| 项目介绍文档 | 本文档 |
| 实时前置环境搭建文档 | 描述 MySQL、Kafka、Flink、Doris 等组件部署流程 |
| 模拟数据生成脚本 | 向 MySQL 注入业务数据 |
| Flink CDC 作业脚本 | 采集 MySQL 数据到 Kafka |
| Flink 数仓加工作业 | DIM/DWD/DWS/ADS 各层 Flink 任务 |
| 可视化看板 | 基于 Doris 的实时大屏 |

## 八、项目目标

- 构建端到端金融实时数仓，数据从产生到可视化的端到端延迟控制在秒级。
- 形成可复用的实时数仓分层建设模板，可扩展到其他业务域。
- 沉淀 Flink + Doris 实时数仓最佳实践。
